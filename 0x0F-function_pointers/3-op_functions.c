#include "3-calc.h"

/**
 * op_add - function add two integers
 * @a: first integer
 * @b: seconed integer
 * Return: the sum of a and b
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - function subtract two integers
 * @a: first integer
 * @b: seconed integer
 * Return: the difference of a and b
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - function multiply two integers
 * @a: first integer
 * @b: seconed integer
 * Return: the product of a and b
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - function divide two integers
 * @a: first integer
 * @b: seconed integer
 * Return: the result of division of a by b
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - function modulous two integers
 * @a: first interger
 * @b: seconed integer
 * Return: the remsinder of the division of a by b
 */

int op_mod(int a, int b)
{
	return (a % b);
}
