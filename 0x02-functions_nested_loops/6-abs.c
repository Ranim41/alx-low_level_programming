#include "main.h"
/**
 * _abs - function that computes the absolute value of an int
 *@r: the value
 *Description: The standaed libarary provides asimilar funct..
 *
 * Return: (0)
 *
 */
int _abs(int r)
{
	if (r >= 0)

		return (r);
	else
		return (r * -1);
}
